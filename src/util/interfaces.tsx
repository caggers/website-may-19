export interface SectionObj {
	title?: string;
	subtitle?: string;
	text?: Array<string>;
	icon?: string;
	blogs?: Array<string>;
}

export interface SkillsObj {
  title: string,
  subtitle: string,
  text: string,
  icon: string,
  skills: Array<string>
}